seconds(minutes) {
  return minutes * 60; 
}

main() {
  var x, y, s;
  s = seconds(5);
  x = 10 * s;
  y = 0;
  while (5 > y) {
    output y + (10 * s);
    y = y + 1;
  }
  output x + y;
  return 0;
}
